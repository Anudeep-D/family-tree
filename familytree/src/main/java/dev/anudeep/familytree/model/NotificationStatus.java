package dev.anudeep.familytree.model;

public enum NotificationStatus {
    UNREAD,
    READ
    // Considering if DELETED is needed or if records will be physically removed.
    // For now, keeping it simple with UNREAD and READ.
}
